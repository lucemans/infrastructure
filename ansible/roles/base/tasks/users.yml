- name: Create the entrypoint for my main user account
  user:
    name: "{{ user }}"
    home: "{{ home }}"
    shell: /bin/bash
    system: true
  become: true

- name: Give user sudo access
  lineinfile:
    path: /etc/sudoers
    line: "{{ user }} ALL=(ALL) ALL"
  become: true